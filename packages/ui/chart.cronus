## Chart
## source: packages/ui/src/components/chart.tsx
## wave 0 - foundation
## exports: Chart, ChartConfig, ChartSeries, ChartContainer, ChartCursor, ChartLegend, ChartLegendContent, ChartStyle, ChartTooltip, ChartTooltipContent
## slots: chart, chart-cursor

component Chart(label: text) layout:stack style:chart {
  label "Chart"
}

component ChartConfig(label: text) layout:inline style:chart-config {
  label "ChartConfig"
}

component ChartSeries(label: text) layout:inline style:chart-series {
  label "ChartSeries"
}

component ChartContainer(label: text) layout:inline style:chart-container {
  label "ChartContainer"
}

component ChartCursor(label: text) layout:inline style:chart-cursor {
  label "ChartCursor"
}

component ChartLegend(label: text) layout:inline style:chart-legend {
  label "ChartLegend"
}

component ChartLegendContent(label: text) layout:inline style:chart-legend-content {
  label "ChartLegendContent"
}

component ChartStyle(label: text) layout:inline style:chart-style {
  label "ChartStyle"
}

component ChartTooltip(label: text) layout:inline style:chart-tooltip {
  label "ChartTooltip"
}

component ChartTooltipContent(label: text) layout:inline style:chart-tooltip-content {
  label "ChartTooltipContent"
}
