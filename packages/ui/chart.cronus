## Chart
## source: packages/ui/src/components/chart.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: Chart, ChartConfig, ChartSeries, ChartContainer, ChartCursor, ChartLegend, ChartLegendContent, ChartStyle, ChartTooltip, ChartTooltipContent
## slots: chart, chart-cursor

component Chart(label: text, disabled: boolean) layout:stack style:chart {
  label "Chart"
  action "click"
  action "focus"

  test "uses Chart" {
    click "Chart"
  }
  test "disabled Chart" {
    expect visible "Chart"
  }
}

component ChartConfig(label: text, disabled: boolean) layout:inline style:chart-config {
  label "ChartConfig"
  action "click"
}

component ChartSeries(label: text, disabled: boolean) layout:inline style:chart-series {
  label "ChartSeries"
  action "click"
}

component ChartContainer(label: text, disabled: boolean) layout:inline style:chart-container {
  label "ChartContainer"
  action "click"
}

component ChartCursor(label: text, disabled: boolean) layout:inline style:chart-cursor {
  label "ChartCursor"
  action "click"
}

component ChartLegend(label: text, disabled: boolean) layout:inline style:chart-legend {
  label "ChartLegend"
  action "click"
}

component ChartLegendContent(label: text, disabled: boolean) layout:inline style:chart-legend-content {
  label "ChartLegendContent"
  action "click"
}

component ChartStyle(label: text, disabled: boolean) layout:inline style:chart-style {
  label "ChartStyle"
  action "click"
}

component ChartTooltip(label: text, disabled: boolean) layout:inline style:chart-tooltip {
  label "ChartTooltip"
  action "click"
}

component ChartTooltipContent(label: text, disabled: boolean) layout:inline style:chart-tooltip-content {
  label "ChartTooltipContent"
  action "click"
}
