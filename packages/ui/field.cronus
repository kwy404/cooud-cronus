## Field
## source: packages/ui/src/components/field.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: Field, FieldLabel, FieldDescription, FieldError
## slots: field, field-description, field-error, field-label

component Field(label: text, disabled: boolean) layout:stack style:field {
  label "Field"
  action "click"
  action "focus"

  test "uses Field" {
    click "Field"
  }
  test "disabled Field" {
    expect visible "Field"
  }
}

component FieldLabel(label: text, disabled: boolean) layout:inline style:field-label {
  label "FieldLabel"
  action "click"
}

component FieldDescription(label: text, disabled: boolean) layout:inline style:field-description {
  label "FieldDescription"
  action "click"
}

component FieldError(label: text, disabled: boolean) layout:inline style:field-error {
  label "FieldError"
  action "click"
}
