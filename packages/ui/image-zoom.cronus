## ImageZoom
## source: packages/ui/src/components/image-zoom.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: ImageZoomLabels, ImageZoom
## slots: image-zoom, image-zoom-content, image-zoom-indicator

component ImageZoom(label: text, disabled: boolean) layout:inline style:image-zoom {
  label "ImageZoom"
  action "click"
  action "focus"

  test "uses ImageZoom" {
    click "ImageZoom"
  }
  test "disabled ImageZoom" {
    expect visible "ImageZoom"
  }
}

component ImageZoomLabels(label: text, disabled: boolean) layout:inline style:image-zoom-labels {
  label "ImageZoomLabels"
  action "click"
}
