## Shimmer
## source: packages/ui/src/components/shimmer.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: Shimmer
## slots: shimmer

component Shimmer(label: text, disabled: boolean) layout:inline style:shimmer {
  label "Shimmer"
  action "click"
  action "focus"

  test "uses Shimmer" {
    click "Shimmer"
  }
  test "disabled Shimmer" {
    expect visible "Shimmer"
  }
}
