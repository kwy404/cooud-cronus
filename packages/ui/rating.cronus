## Rating
## source: packages/ui/src/components/rating.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: Rating
## slots: rating, rating-item, rating-star
## variants.size: sm, md, lg

component Rating(label: text, disabled: boolean, size: text) layout:inline style:rating+md {
  label "Rating"
  size "md"
  action "click"
  action "focus"

  test "uses Rating" {
    click "Rating"
  }
  test "disabled Rating" {
    expect visible "Rating"
  }
}
