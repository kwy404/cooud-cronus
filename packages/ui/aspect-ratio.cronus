## AspectRatio
## source: packages/ui/src/components/aspect-ratio.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: AspectRatio
## slots: aspect-ratio

component AspectRatio(label: text, disabled: boolean) layout:inline style:aspect-ratio {
  label "AspectRatio"
  action "click"
  action "focus"

  test "uses AspectRatio" {
    click "AspectRatio"
  }
  test "disabled AspectRatio" {
    expect visible "AspectRatio"
  }
}
