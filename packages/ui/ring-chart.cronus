## RingChart
## source: packages/ui/src/components/ring-chart.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: RingChartItem, RingChart
## slots: ring-chart

component RingChart(label: text, disabled: boolean) layout:stack style:ring-chart {
  label "RingChart"
  columns "name, value, status"
  item "Row"
  action "focus"

  test "shows RingChart" {
    expect visible "RingChart"
  }
  test "disabled RingChart" {
    expect visible "RingChart"
  }
}

component RingChartItem(label: text, disabled: boolean) layout:inline style:ring-chart-item {
  label "RingChartItem"
  action "click"
}
