## ScrollProgress
## source: packages/ui/src/components/scroll-progress.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: ScrollProgress
## slots: scroll-progress, scroll-progress-fill, scroll-progress-ring, scroll-progress-value

component ScrollProgress(label: text, disabled: boolean) layout:inline style:scroll-progress {
  label "ScrollProgress"
  action "click"
  action "focus"

  test "uses ScrollProgress" {
    click "ScrollProgress"
  }
  test "disabled ScrollProgress" {
    expect visible "ScrollProgress"
  }
}
