## Calendar
## source: packages/ui/src/components/calendar.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: Calendar

component Calendar(label: text, disabled: boolean) layout:stack style:calendar {
  label "Calendar"
  columns "name, value, status"
  item "Row"
  action "focus"

  test "shows Calendar" {
    expect visible "Calendar"
  }
  test "disabled Calendar" {
    expect visible "Calendar"
  }
}
