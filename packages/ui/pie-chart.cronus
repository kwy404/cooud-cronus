## PieChart
## source: packages/ui/src/components/pie-chart.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: PieChartItem, PieChart
## slots: pie-chart

component PieChart(label: text, disabled: boolean) layout:stack style:pie-chart {
  label "PieChart"
  columns "name, value, status"
  item "Row"
  action "focus"

  test "shows PieChart" {
    expect visible "PieChart"
  }
  test "disabled PieChart" {
    expect visible "PieChart"
  }
}

component PieChartItem(label: text, disabled: boolean) layout:inline style:pie-chart-item {
  label "PieChartItem"
  action "click"
}
