## ProgressiveBlur
## source: packages/ui/src/components/progressive-blur.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: ProgressiveBlur
## slots: progressive-blur
## variants.side: top, bottom

component ProgressiveBlur(label: text, disabled: boolean) layout:inline style:progressive-blur {
  label "ProgressiveBlur"
  action "click"
  action "focus"

  test "uses ProgressiveBlur" {
    click "ProgressiveBlur"
  }
  test "disabled ProgressiveBlur" {
    expect visible "ProgressiveBlur"
  }
}
