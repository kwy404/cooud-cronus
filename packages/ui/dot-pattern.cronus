## DotPattern
## source: packages/ui/src/components/dot-pattern.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: DotPattern
## slots: dot-pattern

component DotPattern(label: text, disabled: boolean) layout:inline style:dot-pattern {
  label "DotPattern"
  action "click"
  action "focus"

  test "uses DotPattern" {
    click "DotPattern"
  }
  test "disabled DotPattern" {
    expect visible "DotPattern"
  }
}
