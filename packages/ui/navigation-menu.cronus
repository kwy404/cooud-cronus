## NavigationMenu
## source: packages/ui/src/components/navigation-menu.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: NavigationMenu, NavigationMenuList, NavigationMenuItem, NavigationMenuTrigger, NavigationMenuContent, NavigationMenuLink, NavigationMenuIndicator, NavigationMenuViewport
## slots: navigation-menu, navigation-menu-content, navigation-menu-indicator, navigation-menu-item, navigation-menu-link, navigation-menu-list, navigation-menu-trigger, navigation-menu-viewport

component NavigationMenu(label: text, disabled: boolean, open: boolean) layout:overlay style:navigation-menu {
  label "NavigationMenu"
  state open: boolean = false
  title "NavigationMenu"
  action "open"
  action "close"
  action "focus"

  test "opens NavigationMenu" {
    click "open"
    click "close"
  }
  test "disabled NavigationMenu" {
    expect visible "NavigationMenu"
  }
}

component NavigationMenuList(label: text, disabled: boolean) layout:inline style:navigation-menu-list {
  label "NavigationMenuList"
  action "click"
}

component NavigationMenuItem(label: text, disabled: boolean) layout:inline style:navigation-menu-item {
  label "NavigationMenuItem"
  action "click"
}

component NavigationMenuTrigger(label: text, disabled: boolean) layout:inline style:navigation-menu-trigger {
  label "NavigationMenuTrigger"
  action "click"
}

component NavigationMenuContent(label: text, disabled: boolean) layout:inline style:navigation-menu-content {
  label "NavigationMenuContent"
  action "click"
}

component NavigationMenuLink(label: text, disabled: boolean) layout:inline style:navigation-menu-link {
  label "NavigationMenuLink"
  action "click"
}

component NavigationMenuIndicator(label: text, disabled: boolean) layout:inline style:navigation-menu-indicator {
  label "NavigationMenuIndicator"
  action "click"
}

component NavigationMenuViewport(label: text, disabled: boolean) layout:inline style:navigation-menu-viewport {
  label "NavigationMenuViewport"
  action "click"
}
