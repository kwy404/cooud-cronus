## SplitButton
## source: packages/ui/src/components/split-button.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: SplitButtonItem, SplitButton
## slots: split-button

component SplitButton(label: text, disabled: boolean) layout:inline style:split-button {
  label "SplitButton"
  action "click"
  action "focus"

  test "uses SplitButton" {
    click "SplitButton"
  }
  test "disabled SplitButton" {
    expect visible "SplitButton"
  }
}

component SplitButtonItem(label: text, disabled: boolean) layout:inline style:split-button-item {
  label "SplitButtonItem"
  action "click"
}
