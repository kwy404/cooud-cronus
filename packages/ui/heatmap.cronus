## Heatmap
## source: packages/ui/src/components/heatmap.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: HeatmapDay, Heatmap
## slots: heatmap, heatmap-day, heatmap-legend, heatmap-legend-swatch

component Heatmap(label: text, disabled: boolean) layout:inline style:heatmap {
  label "Heatmap"
  action "click"
  action "focus"

  test "uses Heatmap" {
    click "Heatmap"
  }
  test "disabled Heatmap" {
    expect visible "Heatmap"
  }
}

component HeatmapDay(label: text, disabled: boolean) layout:inline style:heatmap-day {
  label "HeatmapDay"
  action "click"
}
