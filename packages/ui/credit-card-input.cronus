## CreditCardInput
## source: packages/ui/src/components/credit-card-input.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: CardBrand, CreditCardValue, CreditCardInput
## slots: credit-card-input

component CreditCardInput(label: text, disabled: boolean, value: text, invalid: boolean) layout:inline style:credit-card-input {
  label "CreditCardInput"
  state value: text = ""
  field "CreditCardInput"
  action "focus"

  test "fills CreditCardInput" {
    fill value "x"
  }
  test "disabled CreditCardInput" {
    expect visible "CreditCardInput"
  }
}

component CardBrand(label: text, disabled: boolean) layout:inline style:card-brand {
  label "CardBrand"
  action "click"
}

component CreditCardValue(label: text, disabled: boolean) layout:inline style:credit-card-value {
  label "CreditCardValue"
  action "click"
}
