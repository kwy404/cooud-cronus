## Marquee
## source: packages/ui/src/components/marquee.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: MarqueeMotionPreference, Marquee
## slots: marquee, marquee-group

component Marquee(label: text, disabled: boolean) layout:inline style:marquee {
  label "Marquee"
  action "click"
  action "focus"

  test "uses Marquee" {
    click "Marquee"
  }
  test "disabled Marquee" {
    expect visible "Marquee"
  }
}

component MarqueeMotionPreference(label: text, disabled: boolean) layout:inline style:marquee-motion-preference {
  label "MarqueeMotionPreference"
  action "click"
}
