## Card
## source: packages/ui/src/components/card.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: Card, CardHeader, CardTitle, CardDescription, CardAction, CardContent, CardFooter
## slots: card, card-action, card-content, card-description, card-footer, card-header, card-title

component Card(label: text, disabled: boolean) layout:stack style:card {
  label "Card"
  action "click"
  action "focus"

  test "uses Card" {
    click "Card"
  }
  test "disabled Card" {
    expect visible "Card"
  }
}

component CardHeader(label: text, disabled: boolean) layout:inline style:card-header {
  label "CardHeader"
  action "click"
}

component CardTitle(label: text, disabled: boolean) layout:inline style:card-title {
  label "CardTitle"
  action "click"
}

component CardDescription(label: text, disabled: boolean) layout:inline style:card-description {
  label "CardDescription"
  action "click"
}

component CardAction(label: text, disabled: boolean) layout:inline style:card-action {
  label "CardAction"
  action "click"
}

component CardContent(label: text, disabled: boolean) layout:inline style:card-content {
  label "CardContent"
  action "click"
}

component CardFooter(label: text, disabled: boolean) layout:inline style:card-footer {
  label "CardFooter"
  action "click"
}
