## PasswordInput
## source: packages/ui/src/components/password-input.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: PasswordInput
## slots: password-input, password-input-strength, password-input-toggle

component PasswordInput(label: text, disabled: boolean, value: text, invalid: boolean) layout:inline style:password-input {
  label "PasswordInput"
  state value: text = ""
  field "PasswordInput"
  action "focus"

  test "fills PasswordInput" {
    fill value "x"
  }
  test "disabled PasswordInput" {
    expect visible "PasswordInput"
  }
}
