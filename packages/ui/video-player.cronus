## VideoPlayer
## source: packages/ui/src/components/video-player.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: VideoPlayerLabels, VideoPlayer
## slots: video-player, video-player-controls, video-player-fullscreen, video-player-mute, video-player-overlay-play, video-player-play, video-player-rate, video-player-seek, video-player-time, video-player-video, video-player-volume
## variants.aspect: video, square, wide

component VideoPlayer(label: text, disabled: boolean) layout:inline style:video-player {
  label "VideoPlayer"
  action "click"
  action "focus"

  test "uses VideoPlayer" {
    click "VideoPlayer"
  }
  test "disabled VideoPlayer" {
    expect visible "VideoPlayer"
  }
}

component VideoPlayerLabels(label: text, disabled: boolean) layout:inline style:video-player-labels {
  label "VideoPlayerLabels"
  action "click"
}
