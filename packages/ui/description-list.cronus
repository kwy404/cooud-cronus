## DescriptionList
## source: packages/ui/src/components/description-list.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: DescriptionList, DescriptionTerm, DescriptionDetails, DescriptionItem
## slots: description-details, description-item, description-list, description-term
## variants.layout: stacked, horizontal, grid
## variants.size: sm, md
## variants.striped: true
## variants.bordered: true, false
## variants.detailsAlign: start, end

component DescriptionList(label: text, disabled: boolean, size: text) layout:inline style:description-list+md {
  label "DescriptionList"
  size "md"
  action "click"
  action "focus"

  test "uses DescriptionList" {
    click "DescriptionList"
  }
  test "disabled DescriptionList" {
    expect visible "DescriptionList"
  }
}

component DescriptionTerm(label: text, disabled: boolean) layout:inline style:description-term {
  label "DescriptionTerm"
  action "click"
}

component DescriptionDetails(label: text, disabled: boolean) layout:inline style:description-details {
  label "DescriptionDetails"
  action "click"
}

component DescriptionItem(label: text, disabled: boolean) layout:inline style:description-item {
  label "DescriptionItem"
  action "click"
}
