## Label
## source: packages/ui/src/components/label.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: Label
## slots: label

component Label(label: text, disabled: boolean) layout:inline style:label {
  label "Label"
  action "click"
  action "focus"

  test "uses Label" {
    click "Label"
  }
  test "disabled Label" {
    expect visible "Label"
  }
}
