## RadioGroup
## source: packages/ui/src/components/radio-group.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: RadioGroup, RadioGroupItem
## slots: radio-group, radio-group-item

component RadioGroup(label: text, disabled: boolean, checked: boolean) layout:inline style:radio-group {
  label "RadioGroup"
  state checked: boolean = false
  action "toggle"
  action "focus"

  test "toggles RadioGroup" {
    click "RadioGroup"
  }
  test "disabled RadioGroup" {
    expect visible "RadioGroup"
  }
}

component RadioGroupItem(label: text, disabled: boolean) layout:inline style:radio-group-item {
  label "RadioGroupItem"
  action "click"
}
