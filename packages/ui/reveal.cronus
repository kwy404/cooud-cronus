## Reveal
## source: packages/ui/src/components/reveal.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: Reveal
## slots: reveal

component Reveal(label: text, disabled: boolean) layout:inline style:reveal {
  label "Reveal"
  action "click"
  action "focus"

  test "uses Reveal" {
    click "Reveal"
  }
  test "disabled Reveal" {
    expect visible "Reveal"
  }
}
