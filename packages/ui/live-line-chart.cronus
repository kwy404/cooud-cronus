## LiveLineChart
## source: packages/ui/src/components/live-line-chart.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: LiveLineChart
## slots: live-line-chart

component LiveLineChart(label: text, disabled: boolean) layout:stack style:live-line-chart {
  label "LiveLineChart"
  columns "name, value, status"
  item "Row"
  action "focus"

  test "shows LiveLineChart" {
    expect visible "LiveLineChart"
  }
  test "disabled LiveLineChart" {
    expect visible "LiveLineChart"
  }
}
