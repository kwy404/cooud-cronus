## Checkbox
## source: packages/ui/src/components/checkbox.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: Checkbox
## slots: checkbox

component Checkbox(label: text, disabled: boolean, checked: boolean) layout:inline style:checkbox {
  label "Checkbox"
  state checked: boolean = false
  action "toggle"
  action "focus"

  test "toggles Checkbox" {
    click "Checkbox"
  }
  test "disabled Checkbox" {
    expect visible "Checkbox"
  }
}
