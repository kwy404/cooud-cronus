## Form
## source: packages/ui/src/components/form.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: Form, FormField, FormItem, FormLabel, FormControl, FormDescription, FormMessage
## slots: form-control, form-description, form-item, form-label, form-message

component Form(label: text, disabled: boolean) layout:stack style:form {
  label "Form"
  action "click"
  action "focus"

  test "uses Form" {
    click "Form"
  }
  test "disabled Form" {
    expect visible "Form"
  }
}

component FormField(label: text, disabled: boolean) layout:inline style:form-field {
  label "FormField"
  action "click"
}

component FormItem(label: text, disabled: boolean) layout:inline style:form-item {
  label "FormItem"
  action "click"
}

component FormLabel(label: text, disabled: boolean) layout:inline style:form-label {
  label "FormLabel"
  action "click"
}

component FormControl(label: text, disabled: boolean) layout:inline style:form-control {
  label "FormControl"
  action "click"
}

component FormDescription(label: text, disabled: boolean) layout:inline style:form-description {
  label "FormDescription"
  action "click"
}

component FormMessage(label: text, disabled: boolean) layout:inline style:form-message {
  label "FormMessage"
  action "click"
}
