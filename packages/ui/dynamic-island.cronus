## DynamicIsland
## source: packages/ui/src/components/dynamic-island.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: DynamicIslandView, DynamicIsland
## slots: dynamic-island, dynamic-island-shell, dynamic-island-trigger

component DynamicIsland(label: text, disabled: boolean) layout:inline style:dynamic-island {
  label "DynamicIsland"
  action "click"
  action "focus"

  test "uses DynamicIsland" {
    click "DynamicIsland"
  }
  test "disabled DynamicIsland" {
    expect visible "DynamicIsland"
  }
}

component DynamicIslandView(label: text, disabled: boolean) layout:inline style:dynamic-island-view {
  label "DynamicIslandView"
  action "click"
}
