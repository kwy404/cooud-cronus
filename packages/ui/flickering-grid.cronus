## FlickeringGrid
## source: packages/ui/src/components/flickering-grid.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: FlickeringGrid
## slots: flickering-grid

component FlickeringGrid(label: text, disabled: boolean) layout:inline style:flickering-grid {
  label "FlickeringGrid"
  action "click"
  action "focus"

  test "uses FlickeringGrid" {
    click "FlickeringGrid"
  }
  test "disabled FlickeringGrid" {
    expect visible "FlickeringGrid"
  }
}
