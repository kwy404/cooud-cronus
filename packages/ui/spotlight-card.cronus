## SpotlightCard
## source: packages/ui/src/components/spotlight-card.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: SpotlightCard
## slots: spotlight-card

component SpotlightCard(label: text, disabled: boolean) layout:inline style:spotlight-card {
  label "SpotlightCard"
  action "click"
  action "focus"

  test "uses SpotlightCard" {
    click "SpotlightCard"
  }
  test "disabled SpotlightCard" {
    expect visible "SpotlightCard"
  }
}
