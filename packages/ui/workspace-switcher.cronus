## WorkspaceSwitcher
## source: packages/ui/src/components/workspace-switcher.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: WorkspaceItem, WorkspaceSwitcherLabels, WorkspaceSwitcher
## slots: workspace-switcher, workspace-switcher-content

component WorkspaceSwitcher(label: text, disabled: boolean) layout:inline style:workspace-switcher {
  label "WorkspaceSwitcher"
  action "click"
  action "focus"

  test "uses WorkspaceSwitcher" {
    click "WorkspaceSwitcher"
  }
  test "disabled WorkspaceSwitcher" {
    expect visible "WorkspaceSwitcher"
  }
}

component WorkspaceItem(label: text, disabled: boolean) layout:inline style:workspace-item {
  label "WorkspaceItem"
  action "click"
}

component WorkspaceSwitcherLabels(label: text, disabled: boolean) layout:inline style:workspace-switcher-labels {
  label "WorkspaceSwitcherLabels"
  action "click"
}
