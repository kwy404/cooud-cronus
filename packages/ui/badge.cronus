## Badge
## source: packages/ui/src/components/badge.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: Badge
## slots: badge
## variants.variant: default, primary, secondary, outline, success, warning, destructive, error, info

component Badge(label: text, disabled: boolean, variant: text) layout:inline style:badge {
  label "Badge"
  variant "default"
  action "click"
  action "focus"

  test "uses Badge" {
    click "Badge"
  }
  test "disabled Badge" {
    expect visible "Badge"
  }
}
