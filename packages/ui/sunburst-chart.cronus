## SunburstChart
## source: packages/ui/src/components/sunburst-chart.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: SunburstNode, SunburstChart
## slots: sunburst-chart

component SunburstChart(label: text, disabled: boolean) layout:stack style:sunburst-chart {
  label "SunburstChart"
  columns "name, value, status"
  item "Row"
  action "focus"

  test "shows SunburstChart" {
    expect visible "SunburstChart"
  }
  test "disabled SunburstChart" {
    expect visible "SunburstChart"
  }
}

component SunburstNode(label: text, disabled: boolean) layout:inline style:sunburst-node {
  label "SunburstNode"
  action "click"
}
