## Highlighter
## source: packages/ui/src/components/highlighter.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: Highlighter
## slots: highlighter

component Highlighter(label: text, disabled: boolean) layout:inline style:highlighter {
  label "Highlighter"
  action "click"
  action "focus"

  test "uses Highlighter" {
    click "Highlighter"
  }
  test "disabled Highlighter" {
    expect visible "Highlighter"
  }
}
