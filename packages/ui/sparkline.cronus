## Sparkline
## source: packages/ui/src/components/sparkline.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: Sparkline
## slots: sparkline-area, sparkline-bar, sparkline-line

component Sparkline(label: text, disabled: boolean) layout:inline style:sparkline {
  label "Sparkline"
  action "click"
  action "focus"

  test "uses Sparkline" {
    click "Sparkline"
  }
  test "disabled Sparkline" {
    expect visible "Sparkline"
  }
}
