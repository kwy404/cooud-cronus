## TextEffect
## source: packages/ui/src/components/text-effect.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: TextEffectPer, TextEffectPreset, TextEffectTrigger, TextEffect
## slots: text-effect

component TextEffect(label: text, disabled: boolean) layout:inline style:text-effect {
  label "TextEffect"
  action "click"
  action "focus"

  test "uses TextEffect" {
    click "TextEffect"
  }
  test "disabled TextEffect" {
    expect visible "TextEffect"
  }
}

component TextEffectPer(label: text, disabled: boolean) layout:inline style:text-effect-per {
  label "TextEffectPer"
  action "click"
}

component TextEffectPreset(label: text, disabled: boolean) layout:inline style:text-effect-preset {
  label "TextEffectPreset"
  action "click"
}

component TextEffectTrigger(label: text, disabled: boolean) layout:inline style:text-effect-trigger {
  label "TextEffectTrigger"
  action "click"
}
