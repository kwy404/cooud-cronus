## SpinningText
## source: packages/ui/src/components/spinning-text.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: SpinningText
## slots: spinning-text

component SpinningText(label: text, disabled: boolean) layout:inline style:spinning-text {
  label "SpinningText"
  action "click"
  action "focus"

  test "uses SpinningText" {
    click "SpinningText"
  }
  test "disabled SpinningText" {
    expect visible "SpinningText"
  }
}
