## SignaturePad
## source: packages/ui/src/components/signature-pad.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: SignaturePadHandle, SignaturePad
## slots: signature-pad, signature-pad-canvas, signature-pad-hint

component SignaturePad(label: text, disabled: boolean) layout:inline style:signature-pad {
  label "SignaturePad"
  action "click"
  action "focus"

  test "uses SignaturePad" {
    click "SignaturePad"
  }
  test "disabled SignaturePad" {
    expect visible "SignaturePad"
  }
}

component SignaturePadHandle(label: text, disabled: boolean) layout:inline style:signature-pad-handle {
  label "SignaturePadHandle"
  action "click"
}
