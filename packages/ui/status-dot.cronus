## StatusDot
## source: packages/ui/src/components/status-dot.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: StatusDot
## slots: status-dot, status-dot-indicator, status-dot-label, status-dot-ping, status-dot-sr-label
## variants.status: online, offline, busy, away, success, warning, error, info, neutral
## variants.size: xs, sm, md, lg
## variants.ring: true, false

component StatusDot(label: text, disabled: boolean, size: text) layout:inline style:status-dot+md {
  label "StatusDot"
  size "md"
  action "click"
  action "focus"

  test "uses StatusDot" {
    click "StatusDot"
  }
  test "disabled StatusDot" {
    expect visible "StatusDot"
  }
}
