## Skeleton
## source: packages/ui/src/components/skeleton.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: Skeleton
## slots: skeleton

component Skeleton(label: text, disabled: boolean) layout:inline style:skeleton {
  label "Skeleton"
  action "click"
  action "focus"

  test "uses Skeleton" {
    click "Skeleton"
  }
  test "disabled Skeleton" {
    expect visible "Skeleton"
  }
}
