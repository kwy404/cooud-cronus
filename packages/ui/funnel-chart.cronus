## FunnelChart
## source: packages/ui/src/components/funnel-chart.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: FunnelChartItem, FunnelChart
## slots: funnel-chart

component FunnelChart(label: text, disabled: boolean) layout:stack style:funnel-chart {
  label "FunnelChart"
  columns "name, value, status"
  item "Row"
  action "focus"

  test "shows FunnelChart" {
    expect visible "FunnelChart"
  }
  test "disabled FunnelChart" {
    expect visible "FunnelChart"
  }
}

component FunnelChartItem(label: text, disabled: boolean) layout:inline style:funnel-chart-item {
  label "FunnelChartItem"
  action "click"
}
