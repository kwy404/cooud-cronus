## Sonner
## source: packages/ui/src/components/sonner.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: Sonner, Toaster
## slots: toaster

component Sonner(label: text, disabled: boolean) layout:inline style:sonner {
  label "Sonner"
  action "click"
  action "focus"

  test "uses Sonner" {
    click "Sonner"
  }
  test "disabled Sonner" {
    expect visible "Sonner"
  }
}

component Toaster(label: text, disabled: boolean) layout:inline style:toaster {
  label "Toaster"
  action "click"
}
