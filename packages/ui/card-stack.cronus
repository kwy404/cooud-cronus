## CardStack
## source: packages/ui/src/components/card-stack.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: CardStackItem, CardStack
## slots: card-stack, card-stack-item

component CardStack(label: text, disabled: boolean) layout:inline style:card-stack {
  label "CardStack"
  action "click"
  action "focus"

  test "uses CardStack" {
    click "CardStack"
  }
  test "disabled CardStack" {
    expect visible "CardStack"
  }
}

component CardStackItem(label: text, disabled: boolean) layout:inline style:card-stack-item {
  label "CardStackItem"
  action "click"
}
