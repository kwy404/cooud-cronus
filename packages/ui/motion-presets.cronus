## Motion presets
## ease: cubic-bezier(.22,1,.36,1)

component MotionPresets(label: text, variant: text, disabled: boolean) layout:inline style:motion-presets {
  label "Motion presets"
  variant "ease-out-quart"
  action "play"
  test "plays motion" {
    click "Motion presets"
  }
}
