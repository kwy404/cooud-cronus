## ScrambleText
## source: packages/ui/src/components/scramble-text.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: ScrambleTextMotionPreference, ScrambleText
## slots: scramble-text

component ScrambleText(label: text, disabled: boolean) layout:inline style:scramble-text {
  label "ScrambleText"
  action "click"
  action "focus"

  test "uses ScrambleText" {
    click "ScrambleText"
  }
  test "disabled ScrambleText" {
    expect visible "ScrambleText"
  }
}

component ScrambleTextMotionPreference(label: text, disabled: boolean) layout:inline style:scramble-text-motion-preference {
  label "ScrambleTextMotionPreference"
  action "click"
}
