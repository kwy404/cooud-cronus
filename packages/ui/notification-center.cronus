## NotificationCenter
## source: packages/ui/src/components/notification-center.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: NotificationItem, NotificationRow, NotificationList, NotificationCenter
## slots: notification-badge, notification-center, notification-empty, notification-footer, notification-icon, notification-list, notification-row, notification-trigger, notification-unread-dot

component NotificationCenter(label: text, disabled: boolean) layout:inline style:notification-center {
  label "NotificationCenter"
  action "click"
  action "focus"

  test "uses NotificationCenter" {
    click "NotificationCenter"
  }
  test "disabled NotificationCenter" {
    expect visible "NotificationCenter"
  }
}

component NotificationItem(label: text, disabled: boolean) layout:inline style:notification-item {
  label "NotificationItem"
  action "click"
}

component NotificationRow(label: text, disabled: boolean) layout:inline style:notification-row {
  label "NotificationRow"
  action "click"
}

component NotificationList(label: text, disabled: boolean) layout:inline style:notification-list {
  label "NotificationList"
  action "click"
}
