## Spinner
## source: packages/ui/src/components/spinner.tsx
## behavior: native component (state, items, tests). Kernel renders.
## exports: Spinner
## slots: spinner
## variants.size: sm, md, lg

component Spinner(label: text, disabled: boolean, size: text) layout:inline style:spinner+md {
  label "Spinner"
  size "md"
  action "click"
  action "focus"

  test "uses Spinner" {
    click "Spinner"
  }
  test "disabled Spinner" {
    expect visible "Spinner"
  }
}
