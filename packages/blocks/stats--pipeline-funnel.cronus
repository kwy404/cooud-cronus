## Block StatsPipelineFunnel
## source: registry/stats--pipeline-funnel.json
## type: registry:block
## behavior: data

component StatsPipelineFunnelBlock(label: text) layout:stack style:stats--pipeline-funnel {
  title "Stats Pipeline Funnel"
  subtitle "PipelineFunnel"
  columns "name, value, status"
  item "Row"
  value "0"
  test "reads StatsPipelineFunnel" {
    expect visible "Stats Pipeline Funnel"
  }
}
