## Block AuthAdapter
## source: registry/auth-adapter.json
## type: registry:lib
## layout: stack
## compose: native-sections

component AuthAdapterBlock(label: text) layout:stack style:auth-adapter {
  meta "stack"
  label "AuthAdapter"
  title "Email is required."
  subtitle "Reset link is missing or expired."
  test "use" {
    expect visible "Email is required."
  }
}
