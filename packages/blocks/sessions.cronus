## Block Sessions
## source: registry/sessions.json
## type: registry:block
## behavior: settings

component SessionsBlock(label: text) layout:stack style:sessions {
  title "Sessions"
  subtitle "Sessions"
  field "Name"
  action "Save"
  test "save settings" {
    click "Save"
  }
}
