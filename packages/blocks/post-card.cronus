## Block PostCard
## source: registry/post-card.json
## type: registry:block
## behavior: card

component PostCardBlock(label: text) layout:stack style:post-card {
  title "Post Card"
  subtitle "Post Card"
  action "Post Card"
  test "use block" {
    click "Post Card"
  }
}
